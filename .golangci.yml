version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - errorlint
    - unused
    - bodyclose
    - gocritic
    - gosec
    - misspell
    - nilerr
    - unconvert
    - unparam
    - whitespace
  settings:
    gosec:
      excludes:
        - G104
    gocritic:
      enabled-tags:
        - diagnostic
        - style
        - performance
      disabled-checks:
        - commentedOutCode
        - unnamedResult
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gosec
          - unparam

formatters:
  enable:
    - gofmt
    - goimports
